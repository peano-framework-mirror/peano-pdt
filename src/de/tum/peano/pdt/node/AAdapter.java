/* This file was generated by SableCC (http://www.sablecc.org/). */

package de.tum.peano.pdt.node;

import java.util.*;
import de.tum.peano.pdt.analysis.*;

@SuppressWarnings("nls")
public final class AAdapter extends PAdapter
{
    private TIdentifier _name_;
    private final LinkedList<TIdentifier> _userDefined_ = new LinkedList<TIdentifier>();
    private final LinkedList<TIdentifier> _predefinedDefined_ = new LinkedList<TIdentifier>();

    public AAdapter()
    {
        // Constructor
    }

    public AAdapter(
        @SuppressWarnings("hiding") TIdentifier _name_,
        @SuppressWarnings("hiding") List<TIdentifier> _userDefined_,
        @SuppressWarnings("hiding") List<TIdentifier> _predefinedDefined_)
    {
        // Constructor
        setName(_name_);

        setUserDefined(_userDefined_);

        setPredefinedDefined(_predefinedDefined_);

    }

    @Override
    public Object clone()
    {
        return new AAdapter(
            cloneNode(this._name_),
            cloneList(this._userDefined_),
            cloneList(this._predefinedDefined_));
    }

    public void apply(Switch sw)
    {
        ((Analysis) sw).caseAAdapter(this);
    }

    public TIdentifier getName()
    {
        return this._name_;
    }

    public void setName(TIdentifier node)
    {
        if(this._name_ != null)
        {
            this._name_.parent(null);
        }

        if(node != null)
        {
            if(node.parent() != null)
            {
                node.parent().removeChild(node);
            }

            node.parent(this);
        }

        this._name_ = node;
    }

    public LinkedList<TIdentifier> getUserDefined()
    {
        return this._userDefined_;
    }

    public void setUserDefined(List<TIdentifier> list)
    {
        this._userDefined_.clear();
        this._userDefined_.addAll(list);
        for(TIdentifier e : list)
        {
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
        }
    }

    public LinkedList<TIdentifier> getPredefinedDefined()
    {
        return this._predefinedDefined_;
    }

    public void setPredefinedDefined(List<TIdentifier> list)
    {
        this._predefinedDefined_.clear();
        this._predefinedDefined_.addAll(list);
        for(TIdentifier e : list)
        {
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._name_)
            + toString(this._userDefined_)
            + toString(this._predefinedDefined_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._name_ == child)
        {
            this._name_ = null;
            return;
        }

        if(this._userDefined_.remove(child))
        {
            return;
        }

        if(this._predefinedDefined_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        if(this._name_ == oldChild)
        {
            setName((TIdentifier) newChild);
            return;
        }

        for(ListIterator<TIdentifier> i = this._userDefined_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((TIdentifier) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        for(ListIterator<TIdentifier> i = this._predefinedDefined_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((TIdentifier) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
